{{- $.Scratch.Add "index" slice -}}
{{- range .Site.Pages -}}
    {{ $params := merge .Params (dict "title" .Title "relUrl" (replace .Permalink .Site.BaseURL "") "logicalName" .LogicalName) }}
    {{- $.Scratch.Add "index" $params -}}
{{- end -}}
{{- $.Scratch.Get "index" | jsonify -}}
